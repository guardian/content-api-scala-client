name: Release Snapshot to Sonatype

on:
  release:
    types: [published]

jobs:
  release_snapshot_sonatype:
#    if: "github.event.release.prerelease"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v3
        with:
          distribution: corretto
          java-version: 8
          cache: sbt
      - name: Release Snapshot to Sonatype
        run: |
          VERSION="v19.2.2-SNAPSHOT"
        # strip leading v if it exists
          if [[ ${VERSION:0:1} == "v" ]] ; then
          VERSION=${VERSION:1}
          fi
        # append snapshot if it doesn't exist
          if [[ ${VERSION: -9} != "-SNAPSHOT" ]] ; then
            echo "Version must end in -SNAPSHOT. Adding -SNAPSHOT suffix"
            VERSION="$VERSION-SNAPSHOT"
          fi
          echo $PGP_SECRET | base64 --decode | gpg --batch --import
          export GPG_TTY=$(tty)
          echo "Releasing version $VERSION Sonatype as snapshot"
#          sbt "clean" "project clientCI" "set isSnapshot := true" "release release-version $VERSION with-defaults"
        env:
          PGP_SECRET: ${{ secrets.PGP_SECRET }}
          PGP_PASSPHRASE: ${{ secrets.PGP_PASSPHRASE }}
          SONATYPE_USERNAME: ${{ secrets.SONATYPE_USERNAME }}
          SONATYPE_PASSWORD: ${{ secrets.SONATYPE_PASSWORD }}
